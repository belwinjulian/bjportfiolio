---
phase: 01-foundation-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/theme-provider.tsx
  - next.config.ts
  - tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Dark theme is applied by default with no flash of light/unstyled content on load"
    - "CSS custom property color tokens (background, foreground, primary, accent, muted, border) are available globally via Tailwind classes"
    - "Page metadata displays correct title and description for SEO"
    - "Dev server starts and renders without errors"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with ThemeProvider, Inter font, metadata"
      contains: "ThemeProvider"
    - path: "app/globals.css"
      provides: "Tailwind v4 imports and dark theme CSS custom property tokens"
      contains: "@theme"
    - path: "components/theme-provider.tsx"
      provides: "Client-side next-themes wrapper component"
      contains: "use client"
    - path: "app/page.tsx"
      provides: "Home page placeholder"
  key_links:
    - from: "app/layout.tsx"
      to: "components/theme-provider.tsx"
      via: "ThemeProvider wrapping children"
      pattern: "ThemeProvider.*defaultTheme.*dark"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "app/globals.css"
      to: "Tailwind utility classes"
      via: "@theme directive defining color tokens"
      pattern: "@theme.*--color-background"
---

<objective>
Scaffold the Next.js 15 project with TypeScript, Tailwind CSS v4, and a complete dark theme system using CSS custom properties and next-themes.

Purpose: Establishes the entire project foundation — toolchain, theme infrastructure, and layout shell — so subsequent plans can build UI components on a working, themed base.
Output: Running Next.js dev server with dark-themed root layout, global CSS tokens, and correct metadata.
</objective>

<execution_context>
@/Users/belwinjulian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/belwinjulian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-hero/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with TypeScript and Tailwind v4</name>
  <files>
    package.json
    next.config.ts
    tsconfig.json
    app/layout.tsx
    app/page.tsx
    app/globals.css
  </files>
  <action>
Run `npx create-next-app@latest . --yes` from the project root directory `/Users/belwinjulian/Desktop/bjportfolio` to scaffold a Next.js 15 project with all recommended defaults (TypeScript, Tailwind CSS v4, App Router, ESLint, Turbopack, src directory: no, import alias: @/*).

IMPORTANT: The project root already has a `.planning/` directory and `.git` — run create-next-app in the current directory (`.`) so it scaffolds into the existing project folder without overwriting `.planning/` or `.git/`.

After scaffolding:
1. Verify `package.json` has `next`, `react`, `react-dom`, `tailwindcss`, `typescript` as dependencies
2. Verify `app/` directory was created (NOT `src/app/`)
3. Verify `next.config.ts` exists
4. Run `npm run dev` briefly to confirm the dev server starts, then stop it

Do NOT modify any generated files yet — Task 2 handles customization.
  </action>
  <verify>
Run `ls app/layout.tsx app/page.tsx app/globals.css next.config.ts tsconfig.json package.json` — all files must exist.
Run `npm run build` — must complete without errors.
  </verify>
  <done>Next.js 15 project scaffolded with TypeScript and Tailwind CSS v4, dev server starts and builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Configure dark theme system with CSS tokens and next-themes</name>
  <files>
    package.json
    app/globals.css
    components/theme-provider.tsx
    app/layout.tsx
    app/page.tsx
  </files>
  <action>
**Step 1: Install next-themes**
Run `npm install next-themes`

**Step 2: Replace `app/globals.css` with dark theme tokens**
Replace the entire content of `app/globals.css` with:

```css
@import "tailwindcss";

@theme {
  --color-background: #0a0a0a;
  --color-foreground: #ededed;
  --color-muted: #a1a1aa;
  --color-muted-foreground: #71717a;
  --color-border: #27272a;
  --color-primary: #3b82f6;
  --color-primary-foreground: #f0f9ff;
  --color-accent: #06b6d4;
  --color-accent-foreground: #ecfeff;
  --color-card: #18181b;
  --color-card-foreground: #fafafa;
}

html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}

body {
  background-color: var(--color-background);
  color: var(--color-foreground);
}
```

Color contrast rationale:
- foreground (#ededed) on background (#0a0a0a) = ~17.5:1 ratio (exceeds WCAG AAA 7:1)
- muted (#a1a1aa) on background (#0a0a0a) = ~8.5:1 ratio (exceeds WCAG AA 4.5:1)
- primary (#3b82f6) on background (#0a0a0a) = ~5.3:1 ratio (meets WCAG AA 4.5:1)

**Step 3: Create `components/theme-provider.tsx`**
Create directory `components/` at project root. Create file:

```tsx
'use client'

import { ThemeProvider as NextThemesProvider } from 'next-themes'
import type { ThemeProviderProps } from 'next-themes'

export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>
}
```

**Step 4: Update `app/layout.tsx`**
Replace the entire content with:

```tsx
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Belwin Julian | Full Stack Developer',
  description: 'Full stack developer portfolio showcasing web applications, technical expertise, and professional experience.',
  openGraph: {
    title: 'Belwin Julian | Full Stack Developer',
    description: 'Full stack developer portfolio showcasing web applications, technical expertise, and professional experience.',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.className} bg-background text-foreground antialiased`}>
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false} forcedTheme="dark">
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
```

Key decisions:
- `suppressHydrationWarning` on `<html>` prevents next-themes hydration mismatch warning
- `forcedTheme="dark"` ensures dark-only mode (per project decision: no theme toggle)
- `enableSystem={false}` ignores system preference (dark only)
- Inter font via next/font/google for optimal loading (self-hosted, no layout shift)
- Antialiased text for smoother rendering on dark backgrounds

**Step 5: Update `app/page.tsx` with minimal placeholder**
Replace content with a simple dark-themed placeholder to verify the theme system works:

```tsx
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-foreground mb-2">Belwin Julian</h1>
        <p className="text-xl text-muted">Full Stack Developer</p>
        <p className="text-sm text-primary mt-4">Theme system active</p>
      </div>
    </main>
  )
}
```

This placeholder verifies that `text-foreground`, `text-muted`, and `text-primary` custom tokens all resolve correctly. Plan 02 will replace this with the real Hero component.
  </action>
  <verify>
1. Run `npm run build` — must complete without errors
2. Run `npm run dev`, then in another terminal: `curl -s http://localhost:3000 | grep -c "Belwin Julian"` — must return at least 1
3. Verify `bg-background` class on body produces `#0a0a0a` background
4. Stop dev server after verification
  </verify>
  <done>Dark theme system fully configured: CSS custom property tokens defined in globals.css, ThemeProvider wrapping app with forced dark mode, layout has Inter font + metadata, placeholder page renders with correct dark theme colors.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors or warnings
2. Dev server shows dark background (#0a0a0a) with light text (#ededed)
3. No flash of light/white content on page load
4. Browser dev tools show CSS custom properties (--color-background, --color-foreground, etc.) on :root
5. Page title shows "Belwin Julian | Full Stack Developer" in browser tab
</verification>

<success_criteria>
- Next.js 15 project with TypeScript and Tailwind CSS v4 builds and runs
- Dark theme tokens available globally via Tailwind classes (bg-background, text-foreground, text-primary, etc.)
- next-themes configured with forced dark mode (no flash, no toggle)
- Inter font loaded via next/font (self-hosted, no layout shift)
- Metadata set for SEO (title, description, Open Graph)
- Smooth scroll enabled with prefers-reduced-motion respect
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hero/01-01-SUMMARY.md`
</output>
